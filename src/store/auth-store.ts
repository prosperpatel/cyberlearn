/**
 * @deprecated The Zustand auth store has been replaced by AuthProvider +
 * useAuth (src/features/auth/context/auth-context.tsx).
 *
 * This file is kept as an empty module so that any remaining references
 * produce a clear compile error rather than a silent runtime failure.
 * Remove all imports of useAuthStore and use useAuth() instead.
 */

export {}
